# HEADER: This file was autogenerated at <%= Time.now.to_s %>
# HEADER: by puppet.  While it can still be managed manually, it
# HEADER: is definitely not recommended.
# NOTE: This file was autogenerated by Puppet
<% if !@ssh_keys.nil? and !@ssh_keys.empty? -%>
<% @ssh_keys.each do |k, v| -%>
<% if !v['options'].nil? and !v['options'].empty? -%>
<%= v['options'].join(',') << " " -%>
<% end -%>
<%= v['type'] %> <%= v['key'] %> <%= k %>_<%= v['type'] %>
<% end -%>
<% end -%>
<% if !@ssh_key.nil? and !@ssh_key.empty? -%>
<% if !@ssh_key['options'].nil? and !@ssh_key['options'].empty? -%>
<%= @ssh_key['options'].join(',') << " " -%>
<% end -%>
<%= @ssh_key['type'] %> <%= @ssh_key['key'] %> <%= @username %>_<%= @ssh_key['type'] %>
<% end -%>